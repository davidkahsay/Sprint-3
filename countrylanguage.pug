extends layout

block content
  h1= `Languages Spoken in ${countryLanguage[0].CountryCode}`
  table
    tr
      th Language
      th Is Official
      th Percentage
    each lang in countryLanguage
      tr
        td= lang.Language
        td= lang.IsOfficial
        td= lang.Percentage
